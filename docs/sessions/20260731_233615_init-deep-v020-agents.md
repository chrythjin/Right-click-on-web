# /init-deep — AGENTS.md v0.2.0 갱신

**Date:** 2026-07-31 23:36:15
**Mode:** update (no `--create-new`)
**Branch:** main @ e9a0e51 (v0.2.0)

## 트리거

사용자가 `/init-deep` 슬래시 커맨드를 호출하여 AGENTS.md를 현재 코드베이스에 맞게 재생성 요청.

## Workflow 실행 내역

### Phase 1: Discovery (concurrent)

**Bash 구조 분석:**
- 총 ~24개 파일 (소스 12 + docs 7 + 메타 5)
- 디렉토리 깊이 최대 2 (`docs/sessions`, `tests/manual`)
- 서브디렉토리 3개 (`icons/`, `tests/manual/`, `docs/` + `docs/sessions/`)
- 코드 파일: JS 4개 (content.js 436, content-main.js 124, popup.js 34, background.js 11) + HTML 3 + CSS 1 + JSON 1 + PNG 2

**기존 AGENTS.md 평가:**
- 82 lines, v0.1.0 (commit 4fa538a) 기반
- `content-main.js` 완전 누락 ← 핵심 갭
- 듀얼 스크립트 아키텍처 미문서화
- CODE MAP 섹션 없음
- 런타임 통계 미언급

**소스 직접 read:** content.js (436), content-main.js (124), background.js (11), popup.js (34), popup.html (37), popup.css (163), popup-preview.html (30), README.md (119), manifest.json (34) — 총 ~858 LOC 정독.

### Phase 2: Scoring

| Path | Score | Action |
|------|-------|--------|
| `.` | ROOT | UPDATE |
| `icons/` | ~1 | SKIP |
| `tests/manual/` | ~5 | SKIP (단일 파일) |
| `docs/` | ~4 | SKIP |
| `docs/sessions/` | ~1 | SKIP |

서브디렉토리 모두 <8 — distinct domain 임계값 미달. 루트만 갱신.

### Phase 3: Generation

루트 AGENTS.md를 v0.2.0 기준으로 전면 갱신 (Edit 도구 사용, Write 아님 — 기존 파일 보존):

- 82 → 117 lines (순증 35)
- 헤더 메타데이터 갱신
- STRUCTURE/WHERE TO LOOK 확장 (content-main.js 항목 추가)
- CODE MAP 신규 섹션 추가
- COMMANDS ZIP 명령 갱신
- ANTI-PATTERNS에 v0.2.0 특화 항목 2개 추가
- LIMITATIONS 구체화
- NOTES에 v0.2.0 노트 5개 추가

### Phase 4: Review

- 첫 Edit이 trailing section 중복을 남기는 이슈 발견 (§31 read) → 두 번째 Edit으로 trailing 잔여 섹션 제거
- 최종 검증: 117 lines, 중복 없음, 모든 v0.2.0 핵심 사항 커버

## 결정 사항

- **서브 AGENTS.md 생성 안 함** — 디렉토리 점수 전부 <8, 부모 AGENTS.md가 모든 핵심 정보 커버
- **CODE MAP 포함** — LSP/codegraph 없지만 content.js (436 lines) + content-main.js (124 lines)는 심볼이 충분히 많아 매뉴얼 라인 번호 매핑이 가치 있음
- **Korean-first 유지** — 사용자/기존 컨벤션 준수, 번역 안 함

## 결과

```
[OK] ./AGENTS.md (82 → 117 lines, v0.2.0 + commit e9a0e51 기준)
```

다음 커밋에 포함 예정.
