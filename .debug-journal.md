# Debug Journal — extension runtime bypass

Started: 2026-08-11T12:15:00+09:00
Goal: 실제 Chrome MV3 확장을 로드하여 차단 페이지의 우클릭·선택·복사 동작을 정상화한다.

## Environment snapshot (Phase 0)

- Runtime: Chrome Manifest V3, no-build vanilla JavaScript extension.
- Entry: `manifest.json` content scripts and `background.js` service worker.
- Browser: `C:\Program Files\Google\Chrome\Application\chrome.exe`.
- Git HEAD: `f608943`; working tree already contains user changes.
- Existing runtime evidence: Playwright browser on `https://ultimate-enable-right-click.github.io/` observed `body oncontextmenu="return false;"` and synthetic `contextmenu` dispatch returned `defaultPrevented: true` before extension injection.
- References read: debugging `00-setup.md`, `02-investigate.md`, `partial-runtime-evidence.md`, `playwright-cli.md`.

## Hypotheses

1. [OPEN] The unpacked extension is not actually loaded into the browser profile — distinguishing evidence: Chrome DevTools target list contains no extension service worker or content-script effects — if true, fix is: load path.
2. [OPEN] The extension loads but its capture interceptor is not registered before the page blocks `contextmenu` — distinguishing evidence: injected target still has `defaultPrevented: true` and no interceptor counter increment — if true, fix is: listener timing.
3. [OPEN] The extension loads and intercepts page listener registration, but a page inline handler or a separate event path still cancels the action — distinguishing evidence: interception is present while the relevant event remains cancelled — if true, fix is: handler removal.

## Failed hypothesis round counter

- Round 1: pending.

## Artifacts to revert

- [ ] `C:\Users\U-N-00658\AppData\Local\Temp\right-click-on-web-debug-profile` — temporary Chrome profile used only with `--remote-debugging-port=9224`; remove after QA.
- [ ] `C:\Users\U-N-00658\AppData\Local\Temp\right-click-on-web-headless-profile` — dedicated headless Chrome profile used for extension injection QA; remove after QA.
- [ ] `C:\Users\U-N-00658\AppData\Local\Temp\right-click-on-web-chrome.log` — Chrome startup diagnostic log; remove after runtime investigation.
- [ ] `C:\Users\U-N-00658\AppData\Local\Temp\right-click-on-web-headless.log` — dedicated headless Chrome startup log; remove after runtime investigation.
- [ ] `.debug-journal.md` — local debugging journal; remove before final delivery.

## Findings

### 2026-08-11T12:12:16+09:00 — baseline blocked-site event behavior

- Source: Playwright `browser_evaluate` on `https://ultimate-enable-right-click.github.io/`.
- Value: `{ "bodyOnContextmenu": "return false;", "before": { "dispatchResult": false, "defaultPrevented": true } }`
- Interpretation: The target page genuinely cancels a cancellable `contextmenu` event before the extension is loaded.
- Refutes/Confirms: establishes baseline for H2/H3; does not decide H1.

### 2026-08-11T12:16:00+09:00 — first unpacked extension launch

- Source: `chrome.exe --user-data-dir=<temporary profile> --remote-debugging-port=9224 --load-extension=<repo>`.
- Value: `{ "ProcessAlive": false, "DebugPortListening": false }`
- Interpretation: The Chrome process exited before the debug endpoint became observable. This does not establish whether the extension did or did not inject.
- Refutes/Confirms: no hypothesis decided.
